name = "email-router-worker"
main = "src/index.ts"
compatibility_date = "2024-10-01"

[[kv_namespaces]]
binding = "ALIAS_CONFIG_CACHE"
id = "replace-with-your-kv-id"

# TODO:
# - Set your Cloudflare account_id and any routes or triggers
# - For example, to expose an HTTP route:
# routes = [{ pattern = "email-router.yourdomain.com/*", zone_name = "yourdomain.com" }]

